<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.project.itda.common.dao.IUserRepository">
	<insert id="insertUser" parameterType="com.project.itda.common.model.UserModel">
	<![CDATA[
		INSERT INTO 
			itda_user (userId, familySeq, userPw, userName, userAge, userSex, userAddress, userPhone, approve) 
		VALUES 
			(#{userId}, familySeq.nexv_val, #{userPw}, #{userName}, #{userAge}, #{userSex}, #{userAddress}, #{userPhone}, #{approve})
	]]>
	</insert>
	
	<select id="countByUserId" parameterType="String" resultType="int">
    	SELECT COUNT(*) FROM itda_user WHERE user_id = #{userId}
  	</select>
</mapper>